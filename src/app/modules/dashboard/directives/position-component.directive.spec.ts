/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PositionComponentDirective } from './position-component.directive';

describe('PositionComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new PositionComponentDirective();
    expect(directive).toBeTruthy();
  });
});
